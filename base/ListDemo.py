#!/usr/bin/env python3# list 列表， 有序, 可重复, 类型可混用的集合_list = list[1, 'ha', True, [1, 2, 3]]"""列表特点:     有序, 可重复, 类型可混用实例方法:     - 创建    - 遍历     - 获取元素[index]    - 查看元素是否存在 in / not in,    - 获取元素位置 list.index(obj, start, end)     - 切片[start:stop:step]    - 排序 sort包方法:    - 长度 len    - 排序 sorted"""# ---------------------------list 实例方法-----------------------------li = [1, 2, 3]li = list([1, 2, 3])li = range(10)print(type(li))li = [i for i in range(10)]